# Keycloak Admin credentials — copy to .env and fill in your values
# Never commit .env to version control

KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme
